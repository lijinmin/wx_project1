<%= form_for(@pay_qrcode_service) do |f| %>
  <% if @pay_qrcode_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pay_qrcode_service.errors.count, "error") %> prohibited this pay_qrcode_service from being saved:</h2>

      <ul>
      <% @pay_qrcode_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :website %><br>
    <%= f.text_field :website %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
